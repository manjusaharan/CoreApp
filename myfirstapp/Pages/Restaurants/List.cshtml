@page
@model ListModel
@{

}
@section footer {
<div><h1>@Model.Message</h1></div>
}
<br />
<form method="GET">
<div class="form-group">
<div class="input-group">
<input type="search" class="form-control" asp-for="SearchTerm" />
<span class="input-group-btn">
<button class="btn btn-default">
    <i class="glyphicon glyphicon-search"></i>
</button>
</span>
</div>
</div>
</form>
<!-- <table class="table">
@foreach(var restaurant in Model.Restaurants)
{
    <tr>
    <td>@restaurant.Name</td>
    <td>@restaurant.Cuisine</td>
    <td>
    <a class="btn btn-lg" asp-page = "./details" asp-route-restaurantId="@restaurant.Id">
    <i class="glyphicon glyphicon-zoom-in"></i>
    </a>
   </td><td>
    <a class="btn btn-lg" asp-page = "./edit" asp-route-restaurantId="@restaurant.Id">
    <i class="glyphicon glyphicon-edit"></i>
    </a>
    </td>
    <td>
    <a class="btn btn-lg" asp-page = "./delete" asp-route-restaurantId="@restaurant.Id">
    <i class="glyphicon glyphicon-trash"></i>
    </a>
    </td>
    </tr>
}
</table> -->
@foreach(var restaurant in Model.Restaurants)
{
    <partial name="_summary" Model="restaurant"></partial>
}
<a asp-page="./edit" class="btn btn-primary">Add New</a>